<h1>Albums</h1>

{{#each album}}
    <h3>{{this.name}}</h3>
    <img src="{{this.images.0.url}}">
    <a href="/albums/{{this.id}}/tracks"><button type="button" class="btn btn-success">Track</button>
</a>

{{/each}}